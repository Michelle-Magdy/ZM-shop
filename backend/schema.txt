User  ( id ,username, email, password, role {'user','admin','delivery','moderator'} , addresses :[{
    label,
    address,
    firstname,
    lastname,
    isDefault: boolean,
    
}])

Product (title, price: number , olderPrice:number , stock :number diplays if lessthan or equal 5 ,size , color, description , productOverview:[{key,value}], coverImage, images[] , isDeleted: boolean, categoryId ,slug)

Reviews (productId, userId, username, date :date, rating: number ,title, description , helpful:number)

Cart (userId, items: [
    {
     productId,
     productName,
     coverImage,
     addToCartPrice,
     quantity,
     deliveryPice
    }
])

WishList (userId, items: [
    {
     productId,
    }
])

Category (title,slug,parentId ,virtualChlidren , image ,depth)

Copoun (userId ,code , discountPercentage ,isActive, expirationDate)


Order (userId , orderNumber , items [{
    productId,
}],

shippingStatus: enum {
    'pending',
    'shipped',
    'completed',
    'canceled'
}
)

FAQ (question,answer)

Contact us using socket don't do it now





