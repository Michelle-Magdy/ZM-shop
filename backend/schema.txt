User  ( id ,username, email, password, role {'user','admin','delivery','vendor'} , addresses :[{
    label,
    address,
    firstname,
    lastname,
    isDefault: boolean,
}])

Product (title, price: number , olderPrice:number , stock :number diplays if lessthan or equal 5  , description , productOverview:[{key,value}], coverImage, images[] , isDeleted: boolean, categoryId ,slug)


FAQ (question,answer)

Reviews (productId, userId, date :date, rating: number ,title, description , helpful:number)


Cart (userId, items: [
    {
     productId,
     addToCartPrice,
     quantity,
     deliveryPrice
    }
])


Category (title,slug,parentId ,virtualChlidren , image )


WishList (userId, items: [
    {
     productId,
    }
])


Copoun (code , discountPercentage ,isActive, expirationDate)

------------------------------- Done ---------------------------------


// strip
Order (userId , orderNumber , items [{
    productId,
}],

shippingStatus: enum {
    'pending',
    'shipped',
    'completed',
    'canceled'
}
)

Offers(images);

routes for top selleing products 

update user profile

//? can be delayed
// socket
Contact us using socket don't do it now

send emails and forget password and refresh token

handle the permissions




